[youtube 影片](https://youtu.be/7FfcfK7ETTM)

學習時間約 6 分鐘


```bash
cd
pwd
ls
mkdir
cp
mv
rm
```

此文章由 AI 彙整產生, 並且套用我的文章風格 skill

<iframe 
width="1235" 
height="772" 
src="https://www.youtube.com/embed/7FfcfK7ETTM" 
title="terminal 基本指令" 
frameborder="0" 
allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
referrerpolicy="strict-origin-when-cross-origin" 
allowfullscreen></iframe>

看了這支影片在介紹 terminal 基本命令, 覺得這幾個真的是剛開始碰 Linux / WSL 最容易用到的咚咚, 順便整理下, 免得每次切資料夾都要靠滑鼠慢慢找 XD

影片是在 Windows 的 D 槽資料夾裡面開 terminal, 如果是 WSL 的話磁碟通常會掛在 `/mnt` 底下, 例如 D 槽會是 `/mnt/d`, C 槽會是 `/mnt/c`

```bash
# WSL 切到 D 槽
cd /mnt/d

# WSL 切到 C 槽
cd /mnt/c
```

如果是 Git Bash 或其他 terminal, 路徑可能又會長得不太一樣, 不確定的時候先用 `pwd` 看目前位置就好

## ls

`ls` 是 list, 用來查看目前資料夾裡面有什麼檔案或資料夾

```bash
ls
```

如果想看詳細資訊可以加上 `-l`

```bash
ls -l
```

通常會看到檔案權限、擁有者、檔案大小、修改時間和檔名, 後面的 `-l` 就是傳給 `ls` 的參數

隱藏檔案通常是以 `.` 開頭, 可以加上 `-a` 看全部

```bash
ls -la
```

## pwd

資料夾切來切去之後, 有時候會忘記目前到底在哪裡, 這時候執行 `pwd` 就會印出完整路徑

```bash
pwd
```

可能會得到這種結果

```text
/mnt/d/workspace
```

這個指令很簡單但滿重要的, 特別是準備執行 `rm` 之前, 最好先確認一下 `pwd`, 不然路徑切錯就會開始刺激了 = =

## whoami

`whoami` 可以查看目前使用的帳號

```bash
whoami
```

在 Linux 或 WSL 裡面常常會切換不同 user, 這時候先看一下自己到底是誰, 遇到權限問題會比較容易判斷

## touch

`touch` 可以建立一個新的空檔案

```bash
touch note.txt
ls
```

這時候應該會看到 `note.txt`, 點開來看裡面什麼都沒有是正常的

如果檔案本來就存在, `touch` 通常不會把內容清掉, 只是更新檔案的修改時間

```bash
touch app.log config.json
ls -l
```

建立好檔案之後, 可以用自己習慣的 editor 打開來編輯, 編輯完再用 `cat` 把內容印出來看看

## cat

`cat` 可以直接把檔案內容輸出到 terminal

```bash
cat note.txt
```

如果檔案是空的, 執行後不會出現任何東西; 有內容的話就會整份印出來

```bash
cat config.json
```

小檔案用這樣看很方便, 如果是幾千行的大檔案就不要直接 `cat`, 不然整個 terminal 會被洗到找不到北, 可以改用 `less` 慢慢看

## rm

`rm` 是 remove, 用來刪除檔案

```bash
rm note.txt
ls
```

這邊要特別注意, Linux 的 `rm` 通常不會丟到資源回收筒, 刪掉之後不一定救得回來, 所以建議先用 `pwd` 和 `ls` 確認位置與檔案

怕手滑可以加上 `-i`, 刪除前會先詢問

```bash
rm -i note.txt
```

要刪除資料夾則需要遞迴處理

```bash
rm -r old-folder
```

`rm -rf` 會強制刪除而且不詢問, 網路上看到這種指令不要直接複製貼上, 尤其前面再加一個 `sudo`, 一個路徑打錯就可以直接開始寫遺書了

## mv

`mv` 可以用來重新命名檔案, 也可以用來移動檔案, 這兩件事其實是同一個指令

重新命名的時候, 第二個參數放新的檔名

```bash
mv old-photo.jpg new-photo.jpg
ls
```

這樣 `old-photo.jpg` 就會消失, 然後看到 `new-photo.jpg`, 不是真的把檔案內容重新做一份, 比較像是把檔案換一個名字

如果第二個參數是一個已經存在的資料夾, 就會變成移動檔案

```bash
mv new-photo.jpg image/
ls image
```

這時候目前資料夾裡面的圖片會不見, 但其實是被移到 `image` 裡面, 進去看就會發現它還在

## cd

`cd` 是 change directory, 用來切換目前所在的資料夾

```bash
# 進入 image 資料夾
cd image

# 回到上一層
cd ..

# 回到剛剛待過的資料夾
cd -
```

`cd ..` 是往上一層, `cd -` 則是回到上一次所在的位置, 這個在 C 槽跟 D 槽之間切來切去滿方便的

也可以直接指定完整路徑

```bash
cd /mnt/c
cd /mnt/d
```

如果資料夾名稱有空白, 記得用引號包住

```bash
cd "my project"
```

不然 terminal 會把空白後面的文字當成另一個參數, 然後就開始噴找不到資料夾

## Ctrl + L

terminal 用久了畫面會被一堆輸出塞滿, 這時候按下 `Ctrl + L` 就可以把目前畫面暫時清空

```text
Ctrl + L
```

這只是清掉畫面, 不會刪除檔案, 也不會把剛剛執行的命令清掉, 不用太緊張

有些環境也可以直接執行下面這個

```bash
clear
```

## -h 說明參數

很多 command line 工具會提供 `-h` 或 `--help` 來查看使用說明, 例如影片裡用 `ffmpeg` 示範

```bash
ffmpeg -h
```

也可以看看 `ls` 的說明

```bash
ls --help
```

不過參數不是每個 command 都完全一樣, `-h` 在 `ffmpeg` 通常是 help, 在 `ls` 則可能代表 human-readable, 所以還是要看該工具自己的說明

## mkdir 和 cp

`mkdir` 是 make directory, 用來建立資料夾

```bash
mkdir image
ls
```

資料夾建立好之後, 可以用 `cp` 複製檔案進去

```bash
cp new-photo.jpg image/
ls -l image
```

跟 `mv` 不一樣, `cp` 複製完之後原本的檔案還會留著, 所以目前資料夾跟 `image` 裡面都會有一份

如果是複製資料夾, 通常要加上 `-r`

```bash
cp -r image image-backup
```

這裡要注意目的地資料夾通常要先存在, 不然有時候 `cp` 會把它當成重新命名來處理, 結果跟想像的不一樣

## 串起來玩看看

最後把影片裡幾個命令串起來, 可以在 `/tmp` 這種暫存位置做個小練習

```bash
# 先切到暫存資料夾練習
cd /tmp

# 確認目前位置與帳號
pwd
whoami

# 建立測試檔案
touch command.txt
cat command.txt

# 建立資料夾並複製一份
mkdir backup
cp command.txt backup/

# 改名
mv command.txt command-renamed.txt

# 看看結果
ls -l
ls -l backup

# 確認後刪除測試檔案
rm -i command-renamed.txt
rm -ri backup
```

如果只想先記住最基本的用法, 大概就是這樣

```text
ls       查看檔案與資料夾
pwd      顯示目前路徑
whoami   顯示目前帳號
touch    建立檔案
cat      印出檔案內容
rm       刪除檔案
mv       重新命名或移動檔案
cd       切換資料夾
mkdir    建立資料夾
cp       複製檔案或資料夾
```

先把這幾個用熟, 之後再慢慢補 `grep`、`find`、`chmod` 和 `chown`, 不然一次記太多最後只會全部混在一起, terminal 也會變得很噁心 ~
